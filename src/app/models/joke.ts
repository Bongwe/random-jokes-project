export class Joke {
    public title: string;
    public setup: string;
    public punchLine: string;

    constructor(title: string, setup: string, punchLine: string){
      this.title = title;
      this.setup = setup;
      this.punchLine = punchLine;
    }

}